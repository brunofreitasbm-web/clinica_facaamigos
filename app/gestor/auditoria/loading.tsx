import { ModuleSkeleton } from "@/components/module-skeleton";

export default function AuditoriaLoading() {
  return <ModuleSkeleton fill={false} title="Auditoria & LGPD" subtitle="Carregando registros de acessos e logs do sistema" />;
}
